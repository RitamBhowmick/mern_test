{
  "name": "mern-machine-test-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -k -n FRONTEND,BACKEND -c green,blue \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}